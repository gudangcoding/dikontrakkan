import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nls.dikontrakkan',
  appName: 'dikontrakkan',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
